## Git Identity

Always use this identity when working with git:

- Name: Jose
- Email: jose@nobul.tech

If the repo doesn't have these set locally, configure them before committing:
```
git config user.name "Jose"
git config user.email "jose@nobul.tech"
```
