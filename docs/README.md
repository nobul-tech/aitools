# Documentation

This directory contains source documentation (PDFs and other formats) and their corresponding markdown conversions, organized by vendor.

## Structure

Each vendor should have its own subdirectory containing:
- Source files (PDFs, etc.)
- Converted markdown files
